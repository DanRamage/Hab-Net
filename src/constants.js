//These are constant defines for the media queries. This is we can compare against
//a number instead of strings. So if I only care about things smaller than medium I
//can do a val < MD instead of comparing multiple strings.
export const XS = 0;
export const SM = 1;
export const MD = 2;
export const LG = 3;
export const XLG = 4;
